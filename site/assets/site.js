document.querySelectorAll('[data-copy]').forEach(button=>{button.addEventListener('click',async()=>{const text=document.getElementById(button.dataset.copy).textContent;const status=button.nextElementSibling;try{await navigator.clipboard.writeText(text);status.textContent='Copied.';}catch{const range=document.createRange();range.selectNodeContents(document.getElementById(button.dataset.copy));const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);status.textContent='Text selected. Use your keyboard to copy.';}});});const select=document.getElementById('environment');if(select)select.addEventListener('change',()=>{const link=document.getElementById('environment-link');link.href='https://github.com/goolamabbas/portable-llm-council/blob/main/llm-council/references/'+select.value+'.md';link.firstChild.textContent='Read '+select.options[select.selectedIndex].text+' execution notes ↗ ';});
